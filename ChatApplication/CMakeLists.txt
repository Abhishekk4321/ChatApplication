set(MIN_CMAKE_VERSION ${CMAKE_VERSION})
cmake_minimum_required(VERSION ${MIN_CMAKE_VERSION})

project(ChatApplicationComponents VERSION 0.1 LANGUAGES CXX)

add_subdirectory(ChatApplicationComponents)
